#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css-combo/bin/csscombo" "$@"
  ret=$?
else 
  node  "$basedir/../css-combo/bin/csscombo" "$@"
  ret=$?
fi
exit $ret
